import styled from "styled-components";
import BackgroundImage from "../../img/bg_stars.gif";

const Container = styled.div`
  min-height: 100vh;
  min-width: 100vw;
  background-color: black;
  background-image: url(${BackgroundImage});
`;

export default Container;
